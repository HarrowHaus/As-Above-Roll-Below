{
  "name": "@aarb/core",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic rules and procedural generation core for As Above, Roll Below.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run test"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  },
  "engines": {"node": ">=22"}
}
