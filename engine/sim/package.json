{
  "name": "@aarb/sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "smoke": "node src/run.mjs --runs=25",
    "batch": "node src/run.mjs --runs=1000 --output=results"
  },
  "engines": {"node": ">=22"}
}
